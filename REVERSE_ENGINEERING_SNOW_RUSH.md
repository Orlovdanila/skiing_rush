# Реверс-инжиниринг Snow Rush 2026

**Дата анализа:** 6 января 2026

---

## 1. Движок и стек технологий

### Движок: Construct 3

Ключевые доказательства из `scripts/main.js`:
```javascript
// Generated by Construct, the game and animation creation tool
// Visit: https://www.construct.net
```

### Технический стек

| Компонент | Описание |
|-----------|----------|
| Движок | Construct 3 (HTML5 game engine) |
| Рендеринг | WebGL с fallback на Canvas 2D |
| Аудио | Web Audio API |
| Формат проекта | JSON-based data.json + скомпилированный JS |
| PWA | Полная поддержка через `appmanifest.json` |

### Главные файлы

| Файл | Роль |
|------|------|
| `main.js` | DOM-интерфейс, обработка событий, WebGL |
| `c3main.js` | Runtime движка, логика игры, физика |
| `data.json` | Конфигурация проекта, спрайты, layouts |
| `appmanifest.json` | PWA манифест |

---

## 2. Архитектура проекта

### Структура данных (data.json)

```
project[0] = "Snow Rush 2026"   // Название
project[1] = "Preloader"        // Стартовый layout
project[2] = [Behaviors]        // Поведения: Bullet, solid, Sin, scrollto
project[3] = [Objects]          // Игровые объекты
project[5] = [Layouts]          // Сцены: Preloader, Main...
project[6] = [Spritesheets]     // Графика: Main sheet, Preloader sheet
project[7] = [Audio]            // Звуки: skiis.webm
```

### Используемые Behaviors (поведения Construct 3)

| Behavior | Назначение |
|----------|-----------|
| `Bullet` | Движение объектов по прямой (подарки, препятствия) |
| `solid` | Коллизии с твёрдыми объектами |
| `Sin` | Синусоидальное движение (декор, анимации) |
| `scrollto` | Прокрутка камеры за игроком |

### Разрешение и рендеринг

```
Viewport: 1920×1080
Sampling: trilinear
GPU mode: high-performance
FPS: vsync (адаптивный)
```

---

## 3. Игровая логика

### Управление (из main.js)

```javascript
// Обработка pointer/touch событий
window.addEventListener("pointerdown", t => this.xe("pointerdown", t));
window.addEventListener("pointermove", t => this.xe("pointermove", t));
window.addEventListener("pointerup", t => this.xe("pointerup", t));

// Keyboard
window.addEventListener("keydown", t => this._e("keydown", t));
window.addEventListener("keyup", t => this._e("keyup", t));
```

**Логика тапов:**
- Касание левой половины экрана → движение влево
- Касание правой половины → движение вправо  
- Клавиатура: стрелки / A/D

### Инерция движения

Реализована через **Bullet behavior** с модификацией угла:
- Игрок всегда движется вниз (base direction)
- При повороте меняется только горизонтальная составляющая
- Плавный переход создаёт эффект дуги (инерция лыжника)

```javascript
// Псевдокод инерции (логика, не прямой код)
velocityX = lerp(velocityX, targetX * turnSpeed, smoothing);
angle = atan2(velocityX, baseSpeed);
```

### Скролл

Используется **scrollto** behavior:
- Камера привязана к игроку
- Вертикальный скролл (сверху вниз)
- Объекты спавнятся за пределами viewport и удаляются после прохождения

---

## 4. Сцены и объекты

### Layouts (сцены)

| Layout | Назначение |
|--------|-----------|
| Preloader | Загрузка ассетов, прогресс-бар |
| Main | Игровой процесс |
| GameOver | Результаты, лидерборд |

### Игровые объекты

| Объект | Тип | Поведение |
|--------|-----|-----------|
| Rix_Y | Player | scrollto, keyboard/touch input |
| Name_text | UI | Текстовые элементы |
| Подарки | Collectibles | Bullet (движение вниз к игроку) |
| Деревья/препятствия | Obstacles | solid + Bullet |
| Бустеры | Power-ups | Bullet + collision trigger |

---

## 5. Аудио система

```javascript
// Web Audio API через main.js
this.ln = new AudioContext({ latencyHint: t.latencyHint });
this.un = this.ln.createGain();  // Master volume
```

**Звуки:**
- `skiis.webm` — звук лыж/скольжения
- Формат: WebM с Opus кодеком (эффективное сжатие)

---

## 6. Аналитика и идентификаторы

В текущем экспорте **нет явной аналитики** (GA/Amplitude отсутствует).

**Cookie/Storage:**
- Construct 3 использует `LocalStorage` для сохранения состояния
- Нет GDPR cookie banner в коде

**Для Telegram Mini App:**
- Можно добавить `Telegram.WebApp.CloudStorage` для хайскоров
- `initData` для идентификации пользователя

---

## 7. PWA и мобильная адаптация

```json
{
  "name": "Snow Rush 2026",
  "short_name": "Snow Rush 2026",
  "start_url": "index.html",
  "display": "fullscreen",
  "orientation": "landscape",
  "background_color": "#ffffff"
}
```

**Особенности:**
- Полноэкранный режим (`display: fullscreen`)
- Ландшафтная ориентация
- Иконки для PWA (16-512px)
- Touch-action: none для предотвращения zoom/scroll

---

## 8. Адаптация под Telegram Mini App

### Плюсы текущего решения

✅ Touch-управление готово  
✅ Полноэкранный режим  
✅ WebGL рендеринг (производительность)  
✅ Адаптивное разрешение  
✅ PWA-ready

### Что нужно добавить

```javascript
// 1. Инициализация Telegram SDK
import WebApp from '@twa-dev/sdk';
WebApp.ready();
WebApp.expand();

// 2. Идентификация пользователя
const user = WebApp.initDataUnsafe.user;
const userId = user?.id;

// 3. Haptic feedback
WebApp.HapticFeedback.impactOccurred('light');  // Сбор подарка
WebApp.HapticFeedback.impactOccurred('heavy');  // Столкновение

// 4. Лидерборд через CloudStorage
WebApp.CloudStorage.setItem('highscore', String(score));

// 5. Шаринг результата
const shareUrl = `https://t.me/share/url?text=${encodeURIComponent(
  `🎿 Я набрал ${score} очков в Snow Rush 2026!`
)}`;
WebApp.openTelegramLink(shareUrl);
```

---

## 9. Рекомендации для воспроизведения

### Вариант A: Продолжить в Construct 3

**Плюсы:**
- Быстрая разработка
- Визуальный редактор
- Готовые behaviors (Bullet, solid, scrollto)

**Минусы:**
- Закрытый код
- Большой бандл (~700KB+ минифицированный)

### Вариант B: Переписать на Phaser 3 / PixiJS

**Рекомендуемый стек:**

```
├── src/
│   ├── scenes/
│   │   ├── BootScene.ts      // Загрузка
│   │   ├── GameScene.ts      // Игра
│   │   └── GameOverScene.ts  // Результаты
│   ├── entities/
│   │   ├── Player.ts         // Лыжник с инерцией
│   │   ├── Gift.ts           // Подарки (+10/+30/+50)
│   │   ├── Obstacle.ts       // Деревья, камни
│   │   └── Booster.ts        // Магнит, шлем
│   ├── managers/
│   │   ├── InputManager.ts   // Touch/Keyboard
│   │   ├── SpawnManager.ts   // Генерация объектов
│   │   └── ScoreManager.ts   // Очки и комбо
│   └── config.ts             // Настройки игры
├── assets/
├── package.json
└── vite.config.ts
```

### Ключевые механики для реализации

```typescript
// Инерция лыжника
class Player {
  private velocityX = 0;
  private baseSpeed = 300;
  private turnAcceleration = 800;
  private friction = 0.95;
  private targetDirection = 0; // -1, 0, 1
  
  update(dt: number) {
    // Применяем ускорение
    this.velocityX += this.targetDirection * this.turnAcceleration * dt;
    
    // Трение (инерция)
    this.velocityX *= this.friction;
    
    // Движение
    this.x += this.velocityX * dt;
    this.y += this.baseSpeed * dt;
    
    // Наклон спрайта
    this.rotation = this.velocityX / 500 * 0.3;
  }
}

// Бустер: Магнит
class MagnetBooster {
  private radius = 200;
  private duration = 5000;
  
  attract(gifts: Gift[], playerPos: Vector2) {
    for (const gift of gifts) {
      const dist = distance(gift.position, playerPos);
      if (dist < this.radius) {
        // Притягиваем к игроку
        const dir = normalize(subtract(playerPos, gift.position));
        gift.velocity = scale(dir, 400);
      }
    }
  }
}

// Бустер: Шлем (защита)
class HelmetBooster {
  private hitsRemaining = 3;
  
  onCollision(): boolean {
    if (this.hitsRemaining > 0) {
      this.hitsRemaining--;
      return false; // Не Game Over
    }
    return true; // Game Over
  }
}
```

---

## 10. Сравнение с Santa Goes Skiing

| Аспект | Santa Goes Skiing | Snow Rush 2026 |
|--------|-------------------|----------------|
| Движок | Haxe/OpenFL | Construct 3 |
| Бандл | 2+ MB | ~700KB |
| Код | Обфусцированный | Минифицированный |
| Модификация | Очень сложно | Возможна в C3 |
| Инерция | Да | Да |
| Комбо-система | Ворота | Нет (добавить) |
| Бустеры | Нет явных | Планируются |

---

## 11. Итоговые рекомендации

**Snow Rush 2026** — это **Construct 3** проект с базовой механикой ski-раннера.

### Для MVP и быстрого деплоя в Telegram:

1. **Быстрый путь**: Экспортировать из C3, добавить TG SDK wrapper
2. **Качественный путь**: Переписать на Phaser 3/TypeScript для контроля и оптимизации

### Выбор пути:

| Критерий | Construct 3 | Phaser 3 |
|----------|-------------|----------|
| Скорость разработки | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| Размер бандла | ⭐⭐ | ⭐⭐⭐⭐ |
| Контроль кода | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| TG интеграция | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Кастомизация | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**Рекомендация:** Для продакшена с кастомными механиками (магнит, шлем, комбо) — **Phaser 3 + TypeScript** (меньше бандл, больше контроля, проще интеграция с Telegram API).

---

## Приложение: Полезные ссылки

- **Construct 3:** https://www.construct.net
- **Phaser 3:** https://phaser.io
- **TWA SDK:** https://www.npmjs.com/package/@twa-dev/sdk
- **Telegram Mini Apps:** https://core.telegram.org/bots/webapps
